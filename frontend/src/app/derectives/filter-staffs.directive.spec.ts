import { FilterStaffsDirective } from './filter-staffs.directive';

describe('FilterStaffsDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterStaffsDirective();
    expect(directive).toBeTruthy();
  });
});
