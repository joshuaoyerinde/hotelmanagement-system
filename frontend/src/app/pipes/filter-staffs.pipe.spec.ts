import { FilterStaffsPipe } from './filter-staffs.pipe';

describe('FilterStaffsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterStaffsPipe();
    expect(pipe).toBeTruthy();
  });
});
